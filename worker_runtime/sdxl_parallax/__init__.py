"""SDXL + parallax animation backend — laptop-friendly path for Shorts/Reels.

Generates low-poly stills with SDXL-Turbo (fits in 4GB with CPU offload),
then animates them with 2.5D parallax (Ken Burns zoom+pan). Ships videos
in 15-30s per clip on a GTX 1650.
"""
