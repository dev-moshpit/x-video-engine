# SaaS API service — CPU-only deps.
#
# This service MUST NOT import anything that lazy-loads SDXL / torch /
# the full xvideo render stack. The api may import the cheap plan-only
# surface (xvideo.prompt_native.generate_video_plan, score_plan,
# audit_plan) once PR 4 lands plan preview. Heavy rendering lives
# in apps/worker.

# Web framework
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
python-dotenv>=1.0

# DB (PR 3)
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# Queue producer (PR 6)
redis>=5.0

# Object storage — presign URLs (PR 7)
boto3>=1.34

# Tests
pytest>=8.0
httpx>=0.27
